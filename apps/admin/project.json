{
    "name": "admin",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "apps/admin/src",
    "projectType": "application",
    "tags": ["type:frontend"],
    "// targets": "to see all targets run: nx show project admin --web",
    "targets": {
        "// build.inputs": "Vite inlines these env vars into the bundle at build time. They must be part of the Nx cache key or a changed VITE_API_URL replays a stale bundle pointing at a dead host. Mirrors the @nx/vite inferred inputs + the env entries.",
        "build": {
            "inputs": [
                "default",
                "^default",
                { "externalDependencies": ["vite"] },
                { "env": "VITE_API_URL" },
                { "env": "VITE_SENTRY_DSN_ADMIN" },
                { "env": "VITE_SENTRY_ENVIRONMENT" },
                { "env": "VITE_SENTRY_RELEASE" }
            ]
        }
    }
}
