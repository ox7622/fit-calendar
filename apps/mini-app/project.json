{
    "name": "mini-app",
    "$schema": "../../node_modules/nx/schemas/project-schema.json",
    "sourceRoot": "apps/mini-app/src",
    "projectType": "application",
    "tags": [],
    "// targets": "to see all targets run: nx show project mini-app --web",
    "targets": {
        "// build.inputs": "Vite inlines these env vars into the bundle at build time. They must be part of the Nx cache key or a changed VITE_API_URL (e.g. a new dev tunnel URL) replays a stale bundle pointing at a dead host. Mirrors the @nx/vite inferred inputs + the env entries.",
        "build": {
            "inputs": [
                "default",
                "^default",
                { "externalDependencies": ["vite"] },
                { "env": "VITE_API_URL" },
                { "env": "VITE_SENTRY_DSN_MINI" },
                { "env": "VITE_SENTRY_ENVIRONMENT" },
                { "env": "VITE_SENTRY_RELEASE" }
            ]
        }
    }
}
