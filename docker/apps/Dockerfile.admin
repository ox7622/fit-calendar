# FitCalendar Admin Panel Dockerfile
FROM node:22-alpine

WORKDIR /app

# Copy built application
COPY dist/apps/admin/ ./

# Install serve for static file serving
RUN npm install -g serve

EXPOSE 4010

# -s flag for SPA routing
CMD ["serve", "-s", ".", "-l", "4010"]
