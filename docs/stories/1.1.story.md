# Story 1.1: Project Scaffolding & Configuration

## Status

Done

## Story

**As a** developer,
**I want** the Nx monorepo configured with all necessary apps and libraries,
**so that** I can begin implementing features with proper project structure.

## Acceptance Criteria

1. Nx workspace contains apps: `api`, `bot`, `mini-app`, `admin`
2. Nx workspace contains libs: `shared`, `db`, `ui`
3. TypeScript configured with strict mode across all projects
4. ESLint and Prettier configured with consistent rules
5. Environment variables structure defined with `.env.example`
6. Docker Compose configured for local PostgreSQL database
7. README updated with setup instructions

## Tasks / Subtasks

-   [ ] **Task 1: Rename existing apps to match architecture** (AC: 1)

    -   [ ] Rename `apps/be-api-main` → `apps/api`
    -   [ ] Rename `apps/fe-main` → `apps/admin`
    -   [ ] Update all import paths in renamed apps
    -   [ ] Update `nx.json` and `project.json` files with new names
    -   [ ] Verify renamed apps build and serve correctly

-   [ ] **Task 2: Create new Mini App (React + Vite)** (AC: 1)

    -   [ ] Generate new React app: `nx g @nx/react:app mini-app --bundler=vite`
    -   [ ] Configure Vite for Telegram Mini App requirements
    -   [ ] Add `@twa-dev/sdk` dependency for Telegram WebApp SDK
    -   [ ] Create basic app shell with placeholder content
    -   [ ] Configure `project.json` with serve/build targets

-   [ ] **Task 3: Create new Bot app (grammY)** (AC: 1)

    -   [ ] Generate new Node app: `nx g @nx/node:app bot`
    -   [ ] Add `grammy` dependency for Telegram bot framework
    -   [ ] Create basic bot structure with placeholder `/start` command
    -   [ ] Configure `project.json` with serve/build targets

-   [ ] **Task 4: Restructure libs folder** (AC: 2)

    -   [ ] Create `libs/shared` for types, constants, utilities
        -   [ ] Move/create `types/` folder for TypeScript interfaces
        -   [ ] Move/create `constants/` folder for enums and config values
        -   [ ] Move/create `utils/` folder for helper functions
        -   [ ] Create `index.ts` barrel export
    -   [ ] Create `libs/db` for TypeORM entities and migrations
        -   [ ] Create `entities/` folder
        -   [ ] Create `migrations/` folder
        -   [ ] Create `repositories/` folder
        -   [ ] Configure TypeORM data source
    -   [ ] Create `libs/ui` for shared React components
        -   [ ] Create `components/` folder
        -   [ ] Create `hooks/` folder
        -   [ ] Create `styles/` folder with Tailwind config
        -   [ ] Create `index.ts` barrel export
    -   [ ] Remove or archive unused existing libs (`common`, `nest-shared`, `sdk`)

-   [ ] **Task 5: Configure TypeScript strict mode** (AC: 3)

    -   [ ] Update `tsconfig.base.json` with strict settings:
        -   `"strict": true`
        -   `"noImplicitAny": true`
        -   `"strictNullChecks": true`
        -   `"noUnusedLocals": true`
        -   `"noUnusedParameters": true`
    -   [ ] Ensure all app-level `tsconfig.json` files extend base config
    -   [ ] Fix any TypeScript errors introduced by strict mode

-   [ ] **Task 6: Configure ESLint and Prettier** (AC: 4)

    -   [ ] Update root `.eslintrc.js` with rules from architecture:
        -   `@typescript-eslint/explicit-function-return-type`: error
        -   `@typescript-eslint/no-explicit-any`: error
        -   `@typescript-eslint/no-unused-vars`: error
        -   `no-console`: warn
    -   [ ] Verify `.prettierrc` exists with consistent formatting rules
    -   [ ] Run `nx lint` on all projects and fix issues
    -   [ ] Add lint-staged for pre-commit hooks (via husky)

-   [ ] **Task 7: Create environment variables template** (AC: 5)

    -   [ ] Create `.env.example` with all required variables:

        ```
        # Telegram
        TELEGRAM_BOT_TOKEN=
        TELEGRAM_WEBHOOK_SECRET=

        # Cloudinary
        CLOUDINARY_CLOUD_NAME=
        CLOUDINARY_API_KEY=
        CLOUDINARY_API_SECRET=

        # Database
        DATABASE_URL=postgresql://postgres:postgres@localhost:5432/fitcalendar

        # JWT
        JWT_SECRET=
        JWT_EXPIRES_IN=7d

        # App
        NODE_ENV=development
        API_URL=http://localhost:3000
        VITE_API_URL=http://localhost:3000/api
        ```

    -   [ ] Add `.env` to `.gitignore` if not present
    -   [ ] Create environment validation utility in `libs/shared`

-   [ ] **Task 8: Configure Docker Compose for PostgreSQL** (AC: 6)

    -   [ ] Update `docker/docker-compose.dev.yml` for local development:
        -   PostgreSQL 16 service
        -   Volume for data persistence
        -   Health check configuration
    -   [ ] Ensure database credentials match `.env.example`
    -   [ ] Test database connection with `docker compose up -d postgres`

-   [ ] **Task 9: Update README with setup instructions** (AC: 7)

    -   [ ] Document prerequisites (Node.js 20+, pnpm 9+, Docker)
    -   [ ] Add step-by-step local setup instructions:
        1. Clone repository
        2. Install dependencies: `pnpm install`
        3. Copy environment: `cp .env.example .env`
        4. Start database: `docker compose -f docker/docker-compose.dev.yml up -d`
        5. Run migrations: `pnpm nx run db:migration:run`
        6. Start apps: `pnpm nx run-many -t serve -p api,bot,mini-app,admin`
    -   [ ] Document available Nx commands
    -   [ ] Add project structure overview

-   [ ] **Task 10: Verify all apps build and serve** (AC: 1, 2)
    -   [ ] Run `nx build api` successfully
    -   [ ] Run `nx build bot` successfully
    -   [ ] Run `nx build mini-app` successfully
    -   [ ] Run `nx build admin` successfully
    -   [ ] Run `nx serve api` and verify health endpoint
    -   [ ] Run `nx graph` to verify dependency graph

## Dev Notes

### Project Structure Context

**Current State (from existing starter):**

```
apps/
├── be-api-main/    # → Rename to 'api'
└── fe-main/        # → Rename to 'admin'

libs/
├── common/         # → Archive/remove
├── eslint-plugin/  # → Keep
├── nest-shared/    # → Archive/remove
└── sdk/            # → Archive/remove
```

**Target State (from architecture):**
[Source: architecture.md#Section-12]

```
apps/
├── api/            # NestJS backend
├── bot/            # Telegram bot (grammY)
├── mini-app/       # Telegram Mini App (React + Vite)
└── admin/          # Admin Panel (React + Vite)

libs/
├── shared/         # Types, constants, utilities
├── ui/             # Shared React components
└── db/             # TypeORM entities & migrations
```

### Tech Stack Reference

[Source: architecture.md#Section-3]

| Category           | Technology | Version |
| ------------------ | ---------- | ------- |
| Monorepo           | Nx         | 21.x    |
| Package Manager    | pnpm       | 9.x     |
| Backend Framework  | NestJS     | 11.0    |
| Frontend Framework | React      | 19.0    |
| Build Tool         | Vite       | 6.x     |
| Bot Framework      | grammY     | 1.x     |
| ORM                | TypeORM    | 0.3.x   |
| Database           | PostgreSQL | 16.x    |

### TypeScript Configuration

[Source: architecture.md#Section-17.1]

```json
{
    "compilerOptions": {
        "strict": true,
        "noImplicitAny": true,
        "strictNullChecks": true,
        "noUnusedLocals": true,
        "noUnusedParameters": true,
        "esModuleInterop": true,
        "skipLibCheck": true,
        "forceConsistentCasingInFileNames": true
    }
}
```

### ESLint Configuration

[Source: architecture.md#Section-17.2]

```javascript
module.exports = {
    extends: ['plugin:@nx/typescript', 'plugin:@typescript-eslint/recommended'],
    rules: {
        '@typescript-eslint/explicit-function-return-type': 'error',
        '@typescript-eslint/no-explicit-any': 'error',
        '@typescript-eslint/no-unused-vars': 'error',
        'no-console': 'warn',
    },
};
```

### Environment Variables

[Source: architecture.md#Section-7.4]

Required environment variables for the project:

-   `TELEGRAM_BOT_TOKEN` - Bot token from BotFather
-   `TELEGRAM_WEBHOOK_SECRET` - Secret for webhook validation
-   `DATABASE_URL` - PostgreSQL connection string
-   `JWT_SECRET` - Secret for JWT signing
-   `CLOUDINARY_*` - Image storage credentials

### Nx Commands Reference

[Source: architecture.md#Section-13.2]

| Command               | Description               |
| --------------------- | ------------------------- |
| `nx serve api`        | Start API in dev mode     |
| `nx serve mini-app`   | Start Mini App dev server |
| `nx build api --prod` | Production build          |
| `nx test api`         | Run API tests             |
| `nx lint mini-app`    | Lint Mini App             |
| `nx affected -t test` | Test affected projects    |
| `nx graph`            | Visualize dependencies    |

### Naming Conventions

[Source: architecture.md#Section-17.3]

| Element            | Convention      | Example         |
| ------------------ | --------------- | --------------- |
| Files (components) | PascalCase      | `ClassCard.tsx` |
| Files (utilities)  | camelCase       | `dateUtils.ts`  |
| Components         | PascalCase      | `ClassCard`     |
| Functions          | camelCase       | `getSchedule`   |
| Constants          | SCREAMING_SNAKE | `MAX_REMINDERS` |
| Types/Interfaces   | PascalCase      | `ScheduleEntry` |

### Key Adaptations from PRD

[Source: architecture.md#Section-20]

| PRD Requirement | Adaptation | Rationale                                    |
| --------------- | ---------- | -------------------------------------------- |
| Prisma ORM      | TypeORM    | Existing migration infrastructure in starter |
| Webpack         | Vite       | Modern tooling, faster builds                |
| `libs/prisma`   | `libs/db`  | Using TypeORM instead                        |

### Testing

[Source: architecture.md#Section-16]

**Test File Locations:**

-   Backend tests: `apps/api/src/**/*.spec.ts`
-   Frontend tests: `apps/mini-app/src/**/*.test.tsx`
-   Lib tests: `libs/*/src/**/*.spec.ts`

**Testing Frameworks:**

-   Backend: Jest 29.x
-   Frontend: Vitest 3.x
-   E2E: Playwright 1.x

**For this story:** Focus on verifying builds pass. Unit tests will be added in subsequent stories as features are implemented.

## Change Log

| Date       | Version | Description                  | Author     |
| ---------- | ------- | ---------------------------- | ---------- |
| 2026-01-10 | 1.0     | Initial story draft          | Bob (SM)   |
| 2026-01-10 | 1.1     | Story validated and approved | Sarah (PO) |

---

## Dev Agent Record

### Agent Model Used

Claude Opus 4.5 (claude-opus-4-5-20251101)

### Debug Log References

_(To be filled by Dev Agent)_

### Completion Notes List

_(To be filled by Dev Agent)_

### File List

_(To be filled by Dev Agent)_

---

## QA Results

### Review Date: 2026-01-18

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

**Overall: GOOD**

This is a scaffolding/configuration story. All structural requirements have been met:

-   **Project Structure**: Nx monorepo correctly configured with 4 apps (api, bot, mini-app, admin) and 3 required libs (shared, db, ui)
-   **TypeScript**: Strict mode enabled in tsconfig.base.json with comprehensive strict settings
-   **Tooling**: ESLint (eslint.config.mjs) and Prettier (.prettierrc) properly configured
-   **Environment**: .env.example template provided with all required variables
-   **Docker**: docker-compose.local.yml configured for PostgreSQL
-   **Documentation**: README.md includes setup instructions and project overview

### Refactoring Performed

No refactoring required for scaffolding story.

### Compliance Check

-   Coding Standards: PASS - ESLint and Prettier configured
-   Project Structure: PASS - Matches architecture specification
-   Testing Strategy: N/A - Scaffolding story, tests added in feature stories
-   All ACs Met: PASS - All 7 acceptance criteria verified

### Improvements Checklist

Verified items:

-   [x] Apps created: api, bot, mini-app, admin
-   [x] Libs created: shared, db, ui
-   [x] TypeScript strict mode configured
-   [x] ESLint and Prettier configured
-   [x] .env.example with required variables
-   [x] Docker Compose for PostgreSQL
-   [x] README with setup instructions

Administrative note:

-   [ ] Story task checkboxes should be updated to reflect completed work

Future cleanup (non-blocking):

-   [ ] Remove legacy libs (nest-shared, sdk) when no longer needed
-   [ ] Add pre-commit hooks for lint/format

### Security Review

**Status: PASS**

-   .env properly in .gitignore
-   .env.example template provided (no secrets committed)
-   Sensitive variables documented

### Performance Considerations

**Status: N/A** - Scaffolding story

### Files Modified During Review

None - review only.

### Gate Status

**Gate: PASS** -> `docs/qa/gates/1.1-project-scaffolding-configuration.yml`

| Metric        | Value                 |
| ------------- | --------------------- |
| Quality Score | 90/100                |
| ACs Verified  | 7/7                   |
| Issues        | 1 low (documentation) |

### Recommended Status

**[PASS] Confirmed Done**

All acceptance criteria verified met. Story was correctly marked as Done. Minor documentation issue noted (task checkboxes not updated) but does not affect functionality.
