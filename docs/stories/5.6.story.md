# Story 5.6: My Reminders List

## Status

ready-for-dev

## Story

**As a** gym member,
**I want** to see a list of all classes I've subscribed to reminders for,
**so that** I can manage my upcoming workout plans.

## Acceptance Criteria

1. `GET /api/v1/reminders` returns the authenticated user's active reminder subscriptions, sorted by class `startTime` ASC (soonest first).
2. The endpoint excludes reminders whose associated class `startTime` is in the past — past classes never appear in the list.
3. Each list item shows: class name, date + time, coach name, and a "Cancel" affordance.
4. `RemindersPage` renders the list, replacing the current placeholder empty state.
5. Loading skeleton (3 rows) is shown while fetching.
6. Empty state — when the user has zero active reminders — shows a friendly message: `"У вас пока нет напоминаний"` with a sub-line `"Откройте занятие и нажмите 'Напомнить'"`.
7. Error state with retry button shown if the fetch fails.
8. Tapping "Cancel" on a reminder calls `DELETE /api/v1/reminders/:id` (Story 5.1 endpoint), removes the row optimistically, and shows a toast `"Напоминание отменено"`. On API failure, the row is restored and an error toast appears.
9. Tapping the body of a reminder row navigates to the corresponding class detail page (`/schedule/:scheduleEntryId`).
10. The list pre-populates `ClassDetailPage`'s subscription state — when the user opens a class they have a reminder for, the button correctly renders as "Cancel Reminder" (closes the gap noted in Story 5.1 Task 7).

## Tasks / Subtasks

-   [ ] **Task 1: Backend — `GET /reminders` endpoint** (AC: 1, 2)

    -   [ ] Add `ReminderService.findActiveByUser(telegramData): Promise<ReminderListItemDto[]>` in `apps/api/src/modules/reminder/reminder.service.ts` (created in Story 5.1)
    -   [ ] Service runs `UserService.upsert` to resolve user, then queries reminders with `relations: ['scheduleEntry', 'scheduleEntry.coach', 'scheduleEntry.trainingType']`
    -   [ ] Filter: `userId = user.id` AND `scheduleEntry.startTime > now()`
    -   [ ] Sort: `scheduleEntry.startTime ASC`
    -   [ ] Return shape: `{ id, scheduleEntryId, status, notifyAt, class: { id, name, startTime, durationMinutes, coachName, coachPhotoUrl } }`

-   [ ] **Task 2: ReminderListItemDto** (AC: 3)

    -   [ ] Create `apps/api/src/modules/reminder/dto/reminder-list-item.dto.ts`
    -   [ ] Nested `class` object includes the fields needed for list rendering (id, name, startTime, durationMinutes, coachName, coachPhotoUrl)
    -   [ ] `@ApiProperty` decorators throughout
    -   [ ] Re-export from the reminder module barrel

-   [ ] **Task 3: Wire `GET /reminders`** (AC: 1)

    -   [ ] In `reminder.controller.ts` (Story 5.1): add `@Get()` returning `ReminderListItemDto[]`
    -   [ ] Already covered by class-level `@UseGuards(TelegramAuthGuard)`; just add the method
    -   [ ] Swagger `@ApiOperation`, `@ApiResponse(200, type: [ReminderListItemDto])`

-   [ ] **Task 4: Frontend — extend `reminders.api.ts`** (AC: 1, 8)

    -   [ ] Add `remindersApi.getList(): Promise<ReminderListItem[]>`
    -   [ ] Export `ReminderListItem` interface mirroring the DTO shape
    -   [ ] `unsubscribe(reminderId)` already exists from Story 5.1; reuse

-   [ ] **Task 5: Replace `RemindersPage` placeholder with real list** (AC: 4, 5, 6, 7, 8, 9)

    -   [ ] Replace contents of `apps/mini-app/src/pages/RemindersPage.tsx`
    -   [ ] Use `useEffect` to fetch on mount; same `cancelled` flag pattern as `CoachSchedulePage`
    -   [ ] Group items by date (matches `CoachSchedulePage.tsx:11-32` `groupByDate` helper) — consider extracting to `apps/mini-app/src/shared/utils/groupByDate.ts` for reuse
    -   [ ] Date headers formatted with `formatDateHeader` (also shared util candidate)
    -   [ ] Each item renders as a card: class name (font-semibold), `HH:mm` time + duration badge, coach thumbnail + name, right-aligned "Cancel" button (`X` icon from lucide-react with text "Отменить")
    -   [ ] Tap on the row body (not the cancel button) → `navigate(/schedule/${scheduleEntryId})`
    -   [ ] Cancel button is `event.stopPropagation()` to avoid bubbling to the row navigation

-   [ ] **Task 6: Optimistic delete with rollback** (AC: 8)

    -   [ ] On Cancel tap: remove the row from local state immediately, show no toast yet
    -   [ ] Call `remindersApi.unsubscribe(id)`
    -   [ ] On success: show toast `"Напоминание отменено"` (reuse Story 5.1 toast utility)
    -   [ ] On failure: re-insert the row at the same index in local state, show error toast `"Не удалось отменить напоминание"`. Use a state copy snapshot taken **before** removal — don't try to reconstruct the order from the server.

-   [ ] **Task 7: Skeleton + empty state** (AC: 5, 6)

    -   [ ] Skeleton: 3 rows of grey shimmer cards matching the real card height (~80px)
    -   [ ] Empty state: centered, `Bell` icon (already imported in current `RemindersPage`), Russian message per AC6, `text-muted-foreground` for the sub-line

-   [ ] **Task 8: Settings access preserved** (AC: 4)

    -   [ ] If Story 5.2 has shipped, `RemindersPage` already has the gear icon header. Keep the gear icon button in the new layout — it sits to the right of the page title.
    -   [ ] If 5.2 hasn't shipped yet, add a `TODO(5.2): Settings gear icon` comment in the header so 5.2 has an obvious insertion point.

-   [ ] **Task 9: Pre-populate `ClassDetailPage` toggle state** (AC: 10)

    -   [ ] Modify `apps/mini-app/src/pages/ClassDetailPage.tsx` (Story 5.1 added the toggle): on mount, after fetching the class, also call `remindersApi.getList()` and find the matching reminder by `scheduleEntryId` to pre-populate the button state
    -   [ ] **OR** (preferred for performance): cache the reminder list in a Zustand store at app load (`useRemindersStore`), and have `ClassDetailPage` read from it. New file: `apps/mini-app/src/shared/stores/remindersStore.ts`. Action: `loadReminders()` fetches and stores; called once on app boot from `AuthProvider` after auth succeeds, plus refreshed when subscribe/unsubscribe runs.
    -   [ ] **Decision:** go with the Zustand store option — it's a small list (per-user reminders, typically <20), it's used by both `RemindersPage` and `ClassDetailPage`, and avoiding the extra `getList` call on every detail-page open is a real win.

-   [ ] **Task 10: Wire the reminders store** (AC: 10)

    -   [ ] `apps/mini-app/src/shared/stores/remindersStore.ts`:
        -   State: `reminders: ReminderListItem[]`, `isLoading: boolean`, `error: string | null`
        -   Actions: `loadReminders()`, `addReminder(item)`, `removeReminder(id)`, `findByScheduleEntry(scheduleEntryId): ReminderListItem | undefined`
    -   [ ] `AuthProvider.tsx` (Story 1.5): after successful auth (user set in store), invoke `loadReminders()` once
    -   [ ] `RemindersPage` reads from the store + triggers `loadReminders()` on mount only if `reminders.length === 0 && !isLoading`
    -   [ ] `ClassDetailPage` derives initial reminder state via `findByScheduleEntry(id)`
    -   [ ] On subscribe success: call `addReminder(reminder)` in addition to local state
    -   [ ] On unsubscribe success: call `removeReminder(id)`
    -   [ ] **Refactor scope:** this means revisiting `ClassDetailPage` button logic from Story 5.1 to read/write through the store instead of local state. Keep the local `isLoading` for the in-flight network call, but the source of truth is the store.

-   [ ] **Task 11: Backend tests** (AC: 1, 2)

    -   [ ] Extend `apps/api/src/modules/reminder/__tests__/reminder.service.spec.ts`:
        -   `findActiveByUser` returns only the calling user's reminders (not other users')
        -   `findActiveByUser` excludes reminders where `scheduleEntry.startTime <= now()`
        -   `findActiveByUser` sorts by `startTime` ASC
        -   `findActiveByUser` returns empty array when user has no reminders
    -   [ ] All tests pass; `pnpm nx test api` should report ≥77 tests after this story

-   [ ] **Task 12: Manual smoke + final checks** (AC: all)

    -   [ ] Subscribe to 3+ classes via `ClassDetailPage`
    -   [ ] Open `/reminders` → verify all show, sorted by date, grouped correctly
    -   [ ] Tap one row → navigates to detail; button should be "Cancel Reminder" (AC10)
    -   [ ] Cancel via list → row disappears, toast shows
    -   [ ] Re-open detail page of cancelled class → button is "Remind Me" again
    -   [ ] `pnpm nx lint api && pnpm nx lint mini-app && pnpm nx build api && pnpm nx build mini-app` all green

## Dev Notes

### Cross-story dependency — Story 7.2 (auth refactor)

Story 7.2 renames `User` → `Customer` and changes auth to require linking. This story's `GET /reminders` endpoint and the `useRemindersStore` need:

-   **If 7.2 ships first** (recommended): `@Customer()` decorator + `RequiresLinkedCustomer` guard. Reminder rows reference `customerId`, not `userId`. The `loadReminders()` call from `AuthProvider` should only fire when `customer !== null` (post-link), not on every auth.
-   **If 5.6 ships first**: the draft as-written (`userId`, no linked-customer gating) works; expect rework when 7.2 lands.

Either way works. Sequence 7.2 → 5.6 if possible.

### Where this fits

5.6 closes the loop on the user-facing reminder UX:

-   5.1 created the subscription mechanism
-   5.2 lets the user choose the reminder offset
-   **5.6 (this) shows them the list and lets them cancel from one place + closes the toggle-state gap that 5.1 deliberately left open**
-   5.3 will deliver the actual notifications

### The ClassDetailPage gap from 5.1

Story 5.1 Task 7 explicitly noted that the button's initial state is `null` (defaults to "Remind Me") with no pre-fetch — because there was no `GET /reminders` endpoint yet. **This story closes that gap (AC10).** The dev should expect to revisit `ClassDetailPage` and refactor it to read from the new `useRemindersStore` instead of local state.

If the dev landing this story finds 5.1 **not yet shipped**, they should still build the GET endpoint, the page, and the store, but they may skip Task 9-10's `ClassDetailPage` integration and leave it for whoever lands 5.1.

### Why a reminders store

The same data is read by two places:

-   `RemindersPage` (the whole list)
-   `ClassDetailPage` (one item lookup by `scheduleEntryId`)

Without a store, every detail-page open would either accept a stale state or do an extra `GET /reminders` round-trip. With a store loaded once on auth, both consumers share the same source of truth and stay in sync after mutations.

The store is per-session — it doesn't need to persist to localStorage. On re-open, auth runs and `loadReminders()` re-fires.

### Endpoint contract

**`GET /api/v1/reminders`** → `200`

```json
[
    {
        "id": "reminder-uuid",
        "scheduleEntryId": "class-uuid",
        "status": "pending",
        "notifyAt": "2026-05-15T09:30:00.000Z",
        "class": {
            "id": "class-uuid",
            "name": "Йога для начинающих",
            "startTime": "2026-05-15T10:00:00.000Z",
            "durationMinutes": 60,
            "coachName": "Мария Иванова",
            "coachPhotoUrl": "https://..."
        }
    }
]
```

Errors: 401 (no/invalid initData). Empty array (not 404) when user has no reminders.

### Service query

```typescript
async findActiveByUser(telegramData: ITelegramUserData): Promise<ReminderListItemDto[]> {
    const user = await this.userService.upsert(telegramData);
    const now = new Date();

    const reminders = await this.reminderRepo
        .createQueryBuilder('r')
        .innerJoinAndSelect('r.scheduleEntry', 'entry')
        .innerJoinAndSelect('entry.coach', 'coach')
        .innerJoinAndSelect('entry.trainingType', 'type')
        .where('r.userId = :userId', { userId: user.id })
        .andWhere('entry.startTime > :now', { now })
        .orderBy('entry.startTime', 'ASC')
        .getMany();

    return reminders.map((r) => this.toListItemDto(r));
}
```

The `createQueryBuilder` form is preferred over `find({ where, relations })` here because the `startTime > now` filter is on a related table — possible with `find` but uglier.

**Note:** if Story 6.2 has already shipped, `createQueryBuilder` is established in the codebase. If not, this story (or 6.2 — whichever lands first) is the first to use it. The existing pattern everywhere else is `repository.find({ where, relations })` — see `apps/api/src/modules/coaches/coaches.service.ts`. Don't fall back to that pattern here; the related-table filter genuinely needs the query builder.

### "Past classes excluded" — automatic but worth re-verifying after 5.3

AC2 filters past classes server-side. Once Story 5.3 (delivery) ships, sent reminders will have `status='sent'` but the underlying class will eventually be in the past — at which point this filter still excludes them. So the list is naturally self-pruning without any cleanup job. The dev should NOT add a separate "completed reminders" view for MVP.

### List row visual layout

```
┌──────────────────────────────────────────────┐
│ Йога для начинающих                          │
│ 10:00 · 60 мин                               │
│ ◯ Мария Иванова                  [✕ Отменить]│
└──────────────────────────────────────────────┘
```

Match the card style from existing `CoachSchedulePage` rows. Coach avatar is a 32px circle (or initials if no photo). Cancel button is a subtle outline variant — destructive feel without being alarming.

### Project Structure Notes

New files:

-   `apps/api/src/modules/reminder/dto/reminder-list-item.dto.ts`
-   `apps/mini-app/src/shared/stores/remindersStore.ts`
-   `apps/mini-app/src/shared/utils/groupByDate.ts` (extracted from `CoachSchedulePage` — optional refactor; if it ends up duplicated for now, that's acceptable)

Updated files:

-   `apps/api/src/modules/reminder/reminder.controller.ts` — add `@Get()`
-   `apps/api/src/modules/reminder/reminder.service.ts` — add `findActiveByUser`
-   `apps/api/src/modules/reminder/index.ts` — re-export new DTO
-   `apps/api/src/modules/reminder/__tests__/reminder.service.spec.ts` — extend
-   `apps/mini-app/src/pages/RemindersPage.tsx` — full replacement
-   `apps/mini-app/src/shared/api/reminders.api.ts` — add `getList`
-   `apps/mini-app/src/shared/stores/index.ts` — re-export `useRemindersStore`
-   `apps/mini-app/src/app/providers/AuthProvider.tsx` — call `loadReminders()` after auth success
-   `apps/mini-app/src/pages/ClassDetailPage.tsx` — refactor button to read/write via store

### Previous Story Insights

-   **From 5.1:** Reminder service, controller, DTOs, button on ClassDetailPage — extend, don't rebuild
-   **From 5.2:** `useUserStore.user.reminderMinutes` is already maintained
-   **From 1.5:** `AuthProvider` is the right place to bootstrap session-level data after auth succeeds — don't add network calls inside random pages

### References

-   API spec: [`docs/architecture/5-api-specification.md` §5.2 — `GET /reminders`]
-   Front-end navigation: [`docs/front-end-spec.md` §2.2]
-   Existing list pattern: `apps/mini-app/src/pages/CoachSchedulePage.tsx`
-   Existing utils to mirror: `groupByDate`, `formatDateHeader` in `CoachSchedulePage.tsx:16-37`
-   Story 5.1: `docs/stories/5.1.story.md` (toggle state gap noted in Task 7)
-   Reminder entity: `libs/db/src/entities/reminder.entity.ts`

## Testing

### Test File Locations

-   Backend: `apps/api/src/modules/reminder/__tests__/reminder.service.spec.ts` (extend)
-   Frontend optional: `apps/mini-app/src/pages/__tests__/RemindersPage.test.tsx`

### Frameworks

-   Backend: Jest 29.x with `@nestjs/testing`
-   Frontend: Vitest 3.x

### Required Backend Tests

See Task 11. Minimum 4 new tests; full suite must remain green.

## Change Log

| Date       | Version | Description                          | Author    |
| ---------- | ------- | ------------------------------------ | --------- |
| 2026-05-01 | 1.0     | Initial story draft (ready-for-dev)                                  | John (PM) |
| 2026-05-01 | 1.1     | Validation patch: noted `createQueryBuilder` is being introduced for the first time (existing codebase uses `repository.find`); affirmed query builder is correct choice for the related-table filter | John (PM) |
| 2026-05-02 | 1.2     | Cross-story dep note: Story 7.2 changes auth model + reminder ownership to customerId; sequence 7.2 first to avoid rework | John (PM) |

---

## Dev Agent Record

### Agent Model Used

_(To be filled by dev agent)_

### Debug Log References

### Completion Notes List

### File List

---

## QA Results

_(To be filled after dev: gate file at `docs/qa/gates/5.6-my-reminders-list.yml`)_
