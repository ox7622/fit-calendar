# Story 6.3: Schedule Management - Create & Edit Classes

## Status

ready-for-dev

## Story

**As a** club administrator,
**I want** to add new classes and edit existing ones,
**so that** I can keep the schedule up to date.

## Acceptance Criteria

1. `POST /api/v1/admin/schedule` creates a new class with `{ trainingTypeId, coachId, startTime, durationMinutes }`. Returns 201 + the created entry.
2. `PUT /api/v1/admin/schedule/:id` updates an existing class. Same fields are editable. Returns 200 + the updated entry.
3. `GET /api/v1/admin/schedule/:id` returns a single class for the edit form to populate.
4. `durationMinutes` allowed values are exactly `30`, `45`, `60`, `90` (matches PRD AC7 of the original story). Other values are rejected with 400.
5. `startTime` must be a valid ISO datetime. The API does not enforce future-only — admins may legitimately log a past class for record-keeping. (Frontend can warn, server doesn't block.)
6. `trainingTypeId` must reference an existing, **active** training type; `coachId` must reference an existing, **active** coach. Otherwise 400.
7. **PUT does NOT change `status`** — cancellation goes through Story 6.4's dedicated endpoint to ensure the cancellation-notification side-effect runs.
8. **PUT triggers a "schedule changed" event** when `startTime` or `durationMinutes` changes. The event payload includes `{ scheduleEntryId, oldStartTime, newStartTime, oldDurationMinutes, newDurationMinutes }` so Story 5.4's listener can notify subscribers.
9. **If `startTime` changes on PUT, every existing pending reminder for that class has its `notifyAt` recomputed** based on each subscribing user's `reminderMinutes`. This keeps reminders aligned with the new class time.
10. Both endpoints protected by `AdminAuthGuard`.
11. Admin "Add Class" page (`/admin/schedule/new`) and "Edit Class" page (`/admin/schedule/:id`) provide forms with the four fields plus Save/Cancel.
12. Form validation matches the API: training type and coach selected from dropdowns of active records, duration is a 4-option select, datetime via native `<input type="datetime-local">`.
13. On save: success toast `"Сохранено"`, navigate back to dashboard. On error: keep form state, show error message.

## Tasks / Subtasks

-   [ ] **Task 1: Install and configure `@nestjs/event-emitter`** (AC: 8)

    -   [ ] `pnpm add @nestjs/event-emitter`
    -   [ ] Register `EventEmitterModule.forRoot()` in `apps/api/src/api.module.ts`
    -   [ ] This is the canonical decoupling mechanism: `AdminScheduleService` emits, Story 5.4's listener consumes. Do not have admin-side code directly call into the bot or reminder service.

-   [ ] **Task 2: Define event constants and payload types** (AC: 8)

    -   [ ] New shared file: `apps/api/src/modules/admin/schedule/schedule.events.ts` (or under `apps/api/src/common/events/` if other event types are likely):
        ```typescript
        export const SCHEDULE_CHANGED_EVENT = 'schedule.changed';
        export interface ScheduleChangedPayload {
            scheduleEntryId: string;
            oldStartTime: Date;
            newStartTime: Date;
            oldDurationMinutes: number;
            newDurationMinutes: number;
        }
        ```
    -   [ ] Story 5.4 will subscribe to this exact event name; the constant is the contract.

-   [ ] **Task 3: Extend `AdminScheduleService`** (AC: 1, 2, 3, 6, 8, 9)

    -   [ ] `findById(id): Promise<AdminScheduleItem>` — for the edit form
    -   [ ] `create(dto): Promise<AdminScheduleItem>` — validate trainingType + coach are active; insert; return
    -   [ ] `update(id, dto): Promise<AdminScheduleItem>` — load existing; compute deltas; save; if `startTime` or `durationMinutes` changed, recompute reminders' `notifyAt` (Task 4) AND emit `SCHEDULE_CHANGED_EVENT`
    -   [ ] Inject `EventEmitter2`

-   [ ] **Task 4: Reminder `notifyAt` recomputation on time change** (AC: 9)

    -   [ ] In the same `update` method (or a private helper): after committing the schedule change, query all `Reminder` rows where `scheduleEntryId = id` AND `status = 'pending'`
    -   [ ] For each, recompute: `notifyAt = newStartTime - user.reminderMinutes minutes`. Need to join the `User` table to read `reminderMinutes` per-row.
    -   [ ] Update in a single batch where possible. **Warning:** the reminder repo lives in the Reminder module — to keep cross-module coupling minimal, expose a method on `ReminderService.recomputeNotifyAtForClass(scheduleEntryId, newStartTime)` and inject `ReminderService` into `AdminScheduleService`.
    -   [ ] If the schedule update succeeds but the reminder recomputation throws, log loudly and continue. Don't fail the admin's save just because of a reminder DB hiccup.

-   [ ] **Task 5: DTOs and validation** (AC: 4, 5, 6)

    -   [ ] `dto/create-schedule-entry.dto.ts`:
        -   `@IsUUID() trainingTypeId`
        -   `@IsUUID() coachId`
        -   `@IsISO8601() startTime` (string at the boundary; transform to Date)
        -   `@IsIn([30, 45, 60, 90]) durationMinutes`
    -   [ ] `dto/update-schedule-entry.dto.ts` — same shape; all fields optional with `@IsOptional()` so PATCH-style is possible (but the route is PUT — frontend always sends all four)
    -   [ ] Reject extra fields (`forbidNonWhitelisted: true` from Story 5.2)

-   [ ] **Task 6: Wire endpoints** (AC: 1, 2, 3, 10)

    -   [ ] In `admin-schedule.controller.ts` (Story 6.2):
        -   `@Post()` → `create(@Body() dto)` → 201
        -   `@Get(':id')` → `findById`
        -   `@Put(':id')` → `update(@Param('id') id, @Body() dto)`
    -   [ ] Swagger decorators for all three

-   [ ] **Task 7: Backend tests — service** (AC: 1, 2, 4, 6, 8, 9)

    -   [ ] Extend `apps/api/src/modules/admin/schedule/__tests__/admin-schedule.service.spec.ts`:
        -   `create` happy path returns the new entry
        -   `create` with inactive coach → 400
        -   `create` with non-existent training type → 400
        -   `update` no-time-change → returns updated, **does NOT emit event**, **does NOT recompute reminders**
        -   `update` with new startTime → emits `SCHEDULE_CHANGED_EVENT` with correct payload
        -   `update` with new startTime → calls `reminderService.recomputeNotifyAtForClass`
        -   `update` PUT with `status` field in body → ignored (status only changes through cancel endpoint)
    -   [ ] Mock `EventEmitter2` and `ReminderService` for assertions

-   [ ] **Task 8: Backend tests — reminder recomputation** (AC: 9)

    -   [ ] In `apps/api/src/modules/reminder/__tests__/reminder.service.spec.ts`:
        -   `recomputeNotifyAtForClass` updates pending reminders to `newStartTime - user.reminderMinutes`
        -   Does not touch `sent` or `failed` reminders
        -   Handles per-user `reminderMinutes` correctly (one user with 15min, one with 60min)

-   [ ] **Task 9: Frontend — admin schedule API client extensions** (AC: 1, 2, 3)

    -   [ ] In `apps/admin/src/shared/api/schedule.api.ts`:
        -   `getById(id): Promise<AdminScheduleItem>`
        -   `create(dto): Promise<AdminScheduleItem>`
        -   `update(id, dto): Promise<AdminScheduleItem>`

-   [ ] **Task 10: Form component** (AC: 11, 12, 13)

    -   [ ] `apps/admin/src/features/schedule/ScheduleForm.tsx` — single component used by both create and edit pages
    -   [ ] Props: `{ initial?: AdminScheduleItem; onSubmit: (dto) => Promise<void>; onCancel: () => void }`
    -   [ ] Hand-rolled controlled state (`useState` for each field) — no `react-hook-form` dep needed for 4 fields
    -   [ ] Coach dropdown: fetches from `/admin/coaches/options` (Story 6.5) — until that lands, the dropdown shows a "Loading..." stub or the dev hardcodes seed UUIDs for local testing. Document the cross-story dependency.
    -   [ ] Training type dropdown: fetches from `/admin/training-types/options` (Story 6.6) — same caveat
    -   [ ] Duration: `<select>` with the four options
    -   [ ] startTime: `<input type="datetime-local">` (HTML5 native is fine for admin — no fancy datepicker lib)
    -   [ ] Submit button disabled while saving; inline spinner; error text below form on failure
    -   [ ] On submit success: toast + navigate to `/admin/dashboard`

-   [ ] **Task 11: New page + Edit page** (AC: 11)

    -   [ ] `apps/admin/src/pages/ScheduleNewPage.tsx` — wraps `ScheduleForm` with no `initial`, calls `create` on submit
    -   [ ] `apps/admin/src/pages/ScheduleEditPage.tsx` — uses `useParams<{ id }>`, fetches via `getById` on mount, passes to `ScheduleForm` as `initial`, calls `update` on submit
    -   [ ] Loading state while fetching the existing record
    -   [ ] Error state if fetch 404s
    -   [ ] Add an "Add Class" button in the dashboard header (`DashboardPage` from 6.2) that navigates to `/admin/schedule/new`

-   [ ] **Task 12: Routing** (AC: 11)

    -   [ ] In `apps/admin/src/app/Router.tsx`:
        -   `/admin/schedule/new` → `ScheduleNewPage` (within `RequireAuth`)
        -   `/admin/schedule/:id` → `ScheduleEditPage` (within `RequireAuth`)

-   [ ] **Task 13: Manual smoke**

    -   [ ] Create a new class via the form → confirm it appears on the dashboard
    -   [ ] Edit a class to change time by an hour → confirm pending reminders' notifyAt shifted by an hour (query DB directly to verify)
    -   [ ] Verify the schedule.changed event fires (add a temporary listener that logs, or watch logs after Story 5.4 lands)
    -   [ ] All builds, tests, lint green

## Dev Notes

### Why this story carefully separates create/edit from cancel

Cancellation has a notification side-effect (Story 5.5) and removes pending reminders. Edit has a different notification side-effect (5.4) and *recomputes* reminders. Conflating them in a single endpoint with `status` updates would force the admin to remember a separate flow for each — and a bug in either branch would cross-contaminate. Two endpoints, two side-effect contracts.

### The `EventEmitter` choice

NestJS's `EventEmitter2` (from `@nestjs/event-emitter`) is the right granularity here:

-   **Why not direct injection of ReminderService?** Couples admin module to reminder module. Story 5.4 doesn't yet exist — and might not, if the user de-scopes. Decoupling via events lets each module own its own concerns.
-   **Why not a real message queue (BullMQ, etc)?** Way too heavy for in-process synchronous side-effects. The whole admin save happens in a single request — no need for durable queues.

The recomputation of reminders (Task 4) is **synchronous and inline**, NOT event-driven. It must complete before the response returns, because the admin would expect their save to leave the system in a consistent state. The event is for the **notification** side-effect (out-of-band) — that one can be async.

### Recompute math

```typescript
// reminder.service.ts (new method)
async recomputeNotifyAtForClass(scheduleEntryId: string, newStartTime: Date): Promise<void> {
    const reminders = await this.reminderRepo.find({
        where: { scheduleEntryId, status: 'pending' },
        relations: ['user'],
    });

    await Promise.all(
        reminders.map((r) =>
            this.reminderRepo.update(r.id, {
                notifyAt: subMinutes(newStartTime, r.user.reminderMinutes),
            })
        )
    );
}
```

A single-statement bulk UPDATE with a SQL expression would be more efficient, but for MVP volumes (low tens of reminders per class) the row-by-row update is clearer and unmissable in code review. Premature optimization is a real cost here.

**Partial-failure mode:** if `Promise.all` rejects partway, some reminders have the new `notifyAt` and others still have the old value. The schedule update has already committed (it ran before this method). Per Task 4's instruction, log the failure and don't fail the admin's save — this leaves the system in an eventually-consistent state where some users get reminders at the new time and others at the old. For MVP this is acceptable; logging surfaces the case so it's visible. The proper fix (transactional with the schedule update, or a retry queue) is post-MVP.

### What the form does NOT do

-   No "recurring class" support (Phase 2)
-   No conflict detection (e.g. "this coach is already teaching at this time" — admin's responsibility for MVP)
-   No drag-to-reschedule on the calendar view
-   No bulk actions

If the dev finds themselves building any of these, stop and raise it.

### Endpoint contracts

**`POST /api/v1/admin/schedule`** body:
```json
{
    "trainingTypeId": "uuid",
    "coachId": "uuid",
    "startTime": "2026-05-04T08:00:00.000Z",
    "durationMinutes": 60
}
```
→ 201 with the created entry (same shape as 6.2 list item).

**`PUT /api/v1/admin/schedule/:id`** — same body, all four fields. Status is read-only via this endpoint. → 200 with the updated entry.

**`GET /api/v1/admin/schedule/:id`** → 200 with the single entry.

Errors: 400 (validation, inactive coach/type), 401 (no/invalid token), 404 (PUT/GET on non-existent id).

### Project Structure Notes

New backend files:

-   `apps/api/src/modules/admin/schedule/schedule.events.ts`
-   `apps/api/src/modules/admin/schedule/dto/create-schedule-entry.dto.ts`
-   `apps/api/src/modules/admin/schedule/dto/update-schedule-entry.dto.ts`
-   (extend) `apps/api/src/modules/reminder/reminder.service.ts` — add `recomputeNotifyAtForClass`

New frontend files:

-   `apps/admin/src/features/schedule/ScheduleForm.tsx`
-   `apps/admin/src/pages/ScheduleNewPage.tsx`
-   `apps/admin/src/pages/ScheduleEditPage.tsx`

Updated:

-   `apps/api/src/api.module.ts` — register `EventEmitterModule`
-   `apps/api/src/modules/admin/schedule/admin-schedule.module.ts` — import `ReminderModule` for service injection
-   `apps/api/src/modules/admin/schedule/admin-schedule.service.ts` — add create/update/findById, inject EventEmitter2 + ReminderService
-   `apps/api/src/modules/admin/schedule/admin-schedule.controller.ts` — add 3 endpoints
-   `apps/admin/src/app/Router.tsx` — add `/schedule/new` and `/schedule/:id` routes
-   `apps/admin/src/pages/DashboardPage.tsx` — add "Add Class" button
-   `apps/admin/src/shared/api/schedule.api.ts` — add CRUD methods

### Previous Story Insights

-   **From 6.2:** Read endpoint and list page already in place
-   **From 5.1/5.6:** Reminder entity, service, store
-   **From 5.3:** `ScheduleModule` (mini-app side schedule.service) is a separate concern; do not modify it
-   **From 6.1:** AdminAuthGuard, admin store, admin api client

### References

-   Architecture data: [`docs/architecture/4-data-models.md` §4.2 ScheduleEntry, Reminder]
-   API spec: [`docs/architecture/5-api-specification.md` §5.3]
-   `@nestjs/event-emitter` docs: https://docs.nestjs.com/techniques/events
-   ScheduleEntry entity: `libs/db/src/entities/schedule-entry.entity.ts`

## Testing

See Tasks 7 + 8. Backend: 10+ new tests. Frontend optional.

## Change Log

| Date       | Version | Description                          | Author    |
| ---------- | ------- | ------------------------------------ | --------- |
| 2026-05-01 | 1.0     | Initial story draft (ready-for-dev)                                  | John (PM) |
| 2026-05-01 | 1.1     | Validation patch: documented partial-failure mode of `recomputeNotifyAtForClass` — `Promise.all` rejection leaves split state | John (PM) |

---

## Dev Agent Record

### Agent Model Used

_(To be filled by dev agent)_

### Debug Log References

### Completion Notes List

### File List

---

## QA Results

_(To be filled after dev: gate file at `docs/qa/gates/6.3-schedule-management-create-edit.yml`)_
