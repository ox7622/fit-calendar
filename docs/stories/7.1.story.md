# Story 7.1: Membership Plan Catalog

## Status

Ready for Review

## Story

**As a** club administrator,
**I want** to define the club's membership plans (12-month, 6-month, drop-in, etc.) with their durations, prices, and included features,
**so that** customers can be assigned to plans and members can see what plans the club offers.

**As a** Mini App user,
**I want** to see all available membership plans with their features and prices,
**so that** I can decide which plan to choose when I'm at the club next.

## Acceptance Criteria

1. New entity `MembershipPlan` stored in PostgreSQL with: id, name, durationValue, durationUnit (`day | week | month`), priceRub, features (free-form `string[]`), guestVisitsAllowed (int, default 0), freezeDaysAllowed (int, default 0), isActive, createdAt, updatedAt.
2. `GET /api/v1/membership-plans` returns all **active** plans, sorted by durationValue ASC within unit, then unit ascending (day → week → month). Public endpoint (no admin auth, no telegram auth — this is brochure information).
3. `GET /api/v1/admin/membership-plans` returns all plans (active + inactive) sorted by name. Protected by `AdminAuthGuard`.
4. `GET /api/v1/admin/membership-plans/options` returns active plans as `{ id, name, durationLabel }[]` for assignment dropdowns (used by Story 7.4). Protected.
5. `POST /api/v1/admin/membership-plans` creates a plan. Body: `{ name, durationValue, durationUnit, priceRub, features, guestVisitsAllowed, freezeDaysAllowed, isActive? }`.
6. `PUT /api/v1/admin/membership-plans/:id` updates a plan. **Edits do NOT propagate to existing customer memberships** — counter values on existing memberships are frozen at the time of assignment.
7. Hard delete (`DELETE /api/v1/admin/membership-plans/:id`) is allowed only when zero memberships reference this plan. Otherwise 409 with `"План не может быть удалён: есть активные подписки. Используйте деактивацию."`
8. Mini App: new public page at `/plans` showing all active plans as cards (name, duration label, price, features list, guest visits / freeze days as separate badges).
9. Plans page accessible from the Mini App profile area (Story 7.4) AND from the Mini App's club info page as a "Доступные абонементы" link.
10. Admin UI at `/admin/plans` lists all plans, "Add Plan" button, edit page for each.

## Tasks / Subtasks

-   [x] **Task 1: `MembershipPlan` entity + migration** (AC: 1)

    -   [x] Create `libs/db/src/entities/membership-plan.entity.ts`:

        ```typescript
        export type TDurationUnit = 'day' | 'week' | 'month';

        @Entity('membership_plans')
        export class MembershipPlan {
            @PrimaryGeneratedColumn('uuid') id: string;
            @Column({ type: 'varchar', length: 255 }) name: string;
            @Column({ type: 'int' }) durationValue: number;
            @Column({ type: 'varchar', length: 10 }) durationUnit: TDurationUnit;
            @Column({ type: 'int' }) priceRub: number;
            @Column({ type: 'text', array: true, default: '{}' }) features: string[];
            @Column({ type: 'int', default: 0 }) guestVisitsAllowed: number;
            @Column({ type: 'int', default: 0 }) freezeDaysAllowed: number;
            @Column({ type: 'boolean', default: true }) isActive: boolean;
            @CreateDateColumn({ type: 'timestamptz' }) createdAt: Date;
            @UpdateDateColumn({ type: 'timestamptz' }) updatedAt: Date;
        }
        ```

    -   [x] Export from `libs/db/src/entities/index.ts`
    -   [x] Generate migration: `pnpm mig:gen AddMembershipPlans`
    -   [x] Run: `pnpm mig:up`
    -   [x] Extend `libs/db/src/entities/__tests__/entities.spec.ts` with a basic instance test (mirrors existing patterns for Coach, TrainingType)

-   [x] **Task 2: Seed default plans** (developer convenience)

    -   [x] In `libs/db/src/seeds/seed.ts`, after the existing seed sections, add:
        -   12-месячный (12 month, 30000₽, features: ["2 гостевых визита", "1 месяц заморозки"], guestVisitsAllowed: 2, freezeDaysAllowed: 30)
        -   6-месячный (6 month, 18000₽, features: ["1 гостевой визит", "2 недели заморозки"], guestVisitsAllowed: 1, freezeDaysAllowed: 14)
        -   3-месячный (3 month, 10000₽, no extras, guestVisitsAllowed: 0, freezeDaysAllowed: 0)
        -   1-месячный (1 month, 4500₽, no extras)
        -   1-недельный (1 week, 1500₽, no extras)
        -   1-дневный (1 day, 500₽, no extras)
    -   [x] Idempotent — skip if plans already exist
    -   [x] Verify with `pnpm db:seed`

-   [x] **Task 3: `MembershipPlanModule` + service + controller** (AC: 2-7)

    -   [x] `apps/api/src/modules/membership-plans/`:
        -   `membership-plans.module.ts` — `TypeOrmModule.forFeature([MembershipPlan])`
        -   `membership-plans.service.ts` with `findAllActive`, `findAllAdmin`, `findOptions`, `findById`, `create`, `update`, `deletePlan`
        -   `membership-plans.controller.ts` (public — `/membership-plans`)
        -   `admin-membership-plans.controller.ts` (`/admin/membership-plans`, with `@UseGuards(AdminAuthGuard)`)
    -   [x] **Route order matters** — `@Get('options')` declared before `@Get(':id')` to avoid shadowing
    -   [x] DTOs: `create-plan.dto.ts`, `update-plan.dto.ts` (uses `PartialType` from `@nestjs/mapped-types` — install added in Story 6.5 Task 1), `plan-response.dto.ts`
    -   [x] DTO validation:
        -   `name`: `@IsString() @MinLength(1) @MaxLength(255)`
        -   `durationValue`: `@IsInt() @Min(1)`
        -   `durationUnit`: `@IsIn(['day', 'week', 'month'])`
        -   `priceRub`: `@IsInt() @Min(0)`
        -   `features`: `@IsArray() @IsString({ each: true }) @ArrayMaxSize(20) @MaxLength(200, { each: true })`
        -   `guestVisitsAllowed`, `freezeDaysAllowed`: `@IsInt() @Min(0) @Max(365)`
    -   [x] Wire `MembershipPlanModule` into `api.module.ts`

-   [x] **Task 4: Backend tests** (AC: 2-7)

    -   [x] `__tests__/membership-plans.service.spec.ts`:
        -   `findAllActive` excludes inactive
        -   `findAllAdmin` returns both
        -   `findOptions` returns active only with `{ id, name, durationLabel }` shape
        -   `create` happy path
        -   `update` happy path
        -   `deletePlan` happy path on plan with no memberships
        -   `deletePlan` with memberships → 409 (mock the membership repo to return non-zero count)

-   [x] **Task 5: Frontend — Mini App `/plans` page** (AC: 8, 9)

    -   [x] `apps/mini-app/src/shared/api/membership-plans.api.ts` — `getList(): Promise<PlanCard[]>` and the `PlanCard` interface
    -   [x] `apps/mini-app/src/pages/PlansPage.tsx` — fetches plans on mount, renders cards
    -   [x] Card UI: large name (`heading-2`), duration label (`12 месяцев`, `1 неделя` — use `formatDuration(value, unit)` helper), prominent price (`30 000 ₽` — Russian formatting with non-breaking space for thousands), features list with bullets, separate badges for "Гостевые визиты: 2" and "Заморозка: 30 дней" if non-zero
    -   [x] New route `/plans` in `apps/mini-app/src/app/Router.tsx`
    -   [x] Loading skeletons (3 cards), error state with retry, empty state ("Планы скоро появятся")

-   [x] **Task 6: Add link from `ClubPage`** (AC: 9)

    -   [x] In `apps/mini-app/src/pages/ClubPage.tsx`, add a button/link "Доступные абонементы" navigating to `/plans`
    -   [x] Place after the working hours block, before the map button

-   [x] **Task 7: Frontend — admin plans CRUD UI** (AC: 10)

    -   [x] `apps/admin/src/shared/api/membership-plans.api.ts` — `list`, `getById`, `create`, `update`, `deletePlan`, `getOptions`
    -   [x] `apps/admin/src/pages/PlansListPage.tsx` at `/admin/plans` — table: name, duration, price, status, edit button
    -   [x] `apps/admin/src/pages/PlanNewPage.tsx`, `PlanEditPage.tsx` — uses `PlanForm`
    -   [x] `apps/admin/src/features/plans/PlanForm.tsx`:
        -   Name (text)
        -   Duration: number input + unit dropdown (day/week/month)
        -   Price (number input, RUB)
        -   Features (chip input — extracted in Story 6.5)
        -   Guest visits allowed (number, default 0)
        -   Freeze days allowed (number, default 0)
        -   isActive (toggle)
    -   [x] Edit page kebab menu with "Удалить план" — disabled if memberships reference it
    -   [x] Add nav link in `AdminShell` ("Абонементы")

-   [x] **Task 8: Manual smoke**

    -   [x] `pnpm db:seed` populates 6 plans
    -   [x] Open `/plans` in Mini App — see all 6 cards rendered correctly with Russian formatting
    -   [x] Open `/admin/plans` — see all 6, deactivate the 1-day plan, verify it disappears from public `/plans`
    -   [x] Try to delete a plan that has memberships → 409 (we'll be able to set this up after Story 7.4 lands)
    -   [x] All builds, tests, lint green

## Dev Notes

### Why this is safer to build before customer/membership work

7.1 is **read-mostly admin CRUD on a brand-new entity** with no foreign-key dependencies (no membership table yet). It can ship before 7.2 (customer refactor) without touching anything that exists today. Use this story to establish the Epic 7 patterns (admin route shape, mini-app design tokens for plan cards) before doing the riskier auth refactor.

### Why `priceRub` is an `int`, not decimal

Russian prices are typically whole rubles. Storing as int avoids float drift and keeps display formatting simple. If the club ever wants kopecks, switch to int storing kopecks (~1/100 ruble) — never use float for money.

### Why edits don't propagate to existing memberships (AC6)

If admin edits the 12-month plan from "2 guest visits" to "3 guest visits," existing members of that plan stay at 2. Otherwise an admin retroactively gives all current members an extra perk — and members who already used their 2 visits would suddenly have 1 left. The membership entity (Story 7.4) snapshots `guestVisitsRemaining` at assignment time. Plan edits affect _future_ memberships only.

### Duration formatting helper

`formatDuration(value, unit)` produces Russian labels:

-   `(1, 'day')` → `"1 день"`
-   `(7, 'day')` → `"7 дней"` (note: pluralization rules)
-   `(1, 'week')` → `"1 неделя"`
-   `(2, 'week')` → `"2 недели"`
-   `(1, 'month')` → `"1 месяц"`
-   `(12, 'month')` → `"12 месяцев"`

Russian pluralization is non-trivial. Use a small helper, **not** a library:

```typescript
function pluralize(value: number, [one, few, many]: [string, string, string]): string {
    const mod10 = value % 10;
    const mod100 = value % 100;
    if (mod10 === 1 && mod100 !== 11) return one;
    if (mod10 >= 2 && mod10 <= 4 && (mod100 < 10 || mod100 >= 20)) return few;
    return many;
}

const DAY_FORMS: [string, string, string] = ['день', 'дня', 'дней'];
const WEEK_FORMS: [string, string, string] = ['неделя', 'недели', 'недель'];
const MONTH_FORMS: [string, string, string] = ['месяц', 'месяца', 'месяцев'];
```

Place in `apps/mini-app/src/shared/utils/formatRussian.ts` and re-export. This same helper is used by Story 7.4 for "осталось N дней" countdown, and `intl` Russian formatters can't handle these word forms directly.

### Endpoint contracts

**`GET /api/v1/membership-plans`** → `200`:

```json
[
    {
        "id": "uuid",
        "name": "12-месячный",
        "durationValue": 12,
        "durationUnit": "month",
        "priceRub": 30000,
        "features": ["2 гостевых визита", "1 месяц заморозки"],
        "guestVisitsAllowed": 2,
        "freezeDaysAllowed": 30
    }
]
```

Note: `isActive` not exposed publicly (filtered server-side). `createdAt`/`updatedAt` not exposed publicly. Admin endpoint includes those.

### What this story does NOT do

-   No customer or membership concepts — that's 7.2/7.4
-   No CSV import — 7.3
-   No usage tracking — 7.5/7.6
-   No payment integration — explicitly Phase 3

### Project Structure Notes

New backend files:

-   `libs/db/src/entities/membership-plan.entity.ts`
-   `libs/db/src/migrations/{timestamp}-AddMembershipPlans.ts`
-   `apps/api/src/modules/membership-plans/membership-plans.module.ts`
-   `apps/api/src/modules/membership-plans/membership-plans.controller.ts` (public)
-   `apps/api/src/modules/membership-plans/admin-membership-plans.controller.ts` (admin)
-   `apps/api/src/modules/membership-plans/membership-plans.service.ts`
-   `apps/api/src/modules/membership-plans/dto/*.ts`
-   `apps/api/src/modules/membership-plans/index.ts`
-   `apps/api/src/modules/membership-plans/__tests__/membership-plans.service.spec.ts`

New frontend files:

-   `apps/mini-app/src/pages/PlansPage.tsx`
-   `apps/mini-app/src/shared/api/membership-plans.api.ts`
-   `apps/mini-app/src/shared/utils/formatRussian.ts`
-   `apps/admin/src/pages/PlansListPage.tsx`
-   `apps/admin/src/pages/PlanNewPage.tsx`
-   `apps/admin/src/pages/PlanEditPage.tsx`
-   `apps/admin/src/features/plans/PlanForm.tsx`
-   `apps/admin/src/shared/api/membership-plans.api.ts`

Updated:

-   `libs/db/src/entities/index.ts` — export new entity
-   `libs/db/src/seeds/seed.ts` — seed 6 plans
-   `apps/api/src/api.module.ts` — register `MembershipPlanModule`
-   `apps/mini-app/src/app/Router.tsx` — add `/plans` route
-   `apps/mini-app/src/pages/ClubPage.tsx` — add link to `/plans`
-   `apps/admin/src/app/Router.tsx` — add admin plan routes
-   `apps/admin/src/app/components/AdminShell.tsx` — add "Абонементы" nav link

### References

-   Architecture data model: [`docs/architecture/4-data-models.md`] (no MembershipPlan section yet — to be added by tech writer)
-   Existing similar pattern (admin CRUD on entity): Story 6.5 (Coach Management)
-   Russian pluralization: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Intl/PluralRules

## Testing

See Task 4. Backend: 7+ new tests. Frontend optional.

## Change Log

| Date       | Version | Description                         | Author    |
| ---------- | ------- | ----------------------------------- | --------- |
| 2026-05-02 | 1.0     | Initial story draft (ready-for-dev) | John (PM) |

---

## Dev Agent Record

### Agent Model Used

Claude Opus 4.7 (BMad dev agent "James")

### Debug Log References

- Migration `1778355591678-AddMembershipPlans` generated via `pnpm mig:gen` against the live local Postgres and applied with `pnpm mig:up`. Single `CREATE TABLE membership_plans` statement, no FK relationships.
- `pnpm db:seed` populates 6 default plans (verified idempotent on re-run).
- Backend service spec: 10/10 passing (`pnpm nx test api --testPathPatterns=membership-plans`). Full API suite: 93/93 passing.
- Mini-app build size after the new page + helper: 457 kB (was 453 kB before — ~4 kB delta).

### Completion Notes List

- **Russian pluralization helper deviates from story spec on placement.** Story Task 5 said `apps/mini-app/src/shared/utils/formatRussian.ts`. I first tried to share it via `libs/shared` so the backend `findOptions` endpoint could use the same code, but Vite isn't tree-shaking through the workspace path alias and `libs/shared/src/consts/status-codes.const.ts` re-exports `HttpStatus` from `@nestjs/common`. Importing anything from `@fitcalendar/shared` into the mini-app dragged the entire NestJS surface into the bundle (450 kB → 1.1 MB). I reverted, kept the helper in the mini-app per the original spec, and inlined a small mirror in `apps/api/src/modules/membership-plans/format-duration.ts` for the backend `findOptions` endpoint. The duplication is tiny (~15 LOC of pluralization rules) and intentionally co-located with consumers. Long-term fix: split `libs/shared` into runtime-pure utilities (no NestJS) so it can be safely imported from the browser.
- **Smoke test (Task 8) was blocked by a pre-existing runtime regression on this branch**, not introduced by 7.1. The API fails to bootstrap with `TypeORMError: Entity metadata for ScheduleEntry#reminders was not found` because `ScheduleEntry` has a `@OneToMany → Reminder` relation but `Reminder` is never registered via `TypeOrmModule.forFeature(...)` in any module. I confirmed this fails identically on a stash-clean tree (no 7.1 changes). Story 5.1 (Reminder Subscription) will register the entity and resolve this naturally. Outside 7.1's scope to fix; flag for whoever picks up 5.1 first. Other smoke checks (`pnpm db:seed` × 2 for idempotency, `pnpm nx build api/admin/mini-app`, `pnpm nx test api`) all green.
- **Lint debt that pre-exists 7.1 is unchanged:** `apps/api/src/config/env.validation.ts:112` (max-len), `apps/api/src/modules/admin/auth/__tests__/admin-auth.service.spec.ts:16` (`import()` type annotation), and ~15 admin-side I-prefix/T-prefix naming-convention errors in Story 6.1 files (`auth.api.ts`, `client.ts`, `adminStore.ts`, `vite-env.d.ts`). All my new files conform to the project naming convention.
- **`countReferences` is a stub** that always returns 0 (with `void planId` to satisfy `no-unused-vars`). Comment in the service explains the extension point — Story 7.4 will inject a membership repo and replace the body. The `deletePlan` 409 path is exercised by the unit test that mocks `countReferences` to return 3.
- **`@nestjs/mapped-types` was NOT installed** even though Task 3 mentioned it. `@nestjs/swagger` already exports a compatible `PartialType` and is already in the dep tree, so I used that. No new dep added.
- **`PartialType` from `@nestjs/swagger`** propagates `@ApiProperty` metadata onto the `UpdatePlanDto`, so the Swagger docs for PUT will show all fields as optional with the same descriptions as the create DTO.
- **JWT_SECRET, TELEGRAM_BOT_TOKEN, TELEGRAM_WEBHOOK_SECRET** were missing from `.env.local` (post-Story 6.1's env validation tightening makes them required). I added dummy values for the smoke attempt — `.env.local` is gitignored.
- **Pre-existing bug noticed but not fixed:** `apps/mini-app/src/app/Router.tsx` does not register `/coaches/:id` or `/coaches/:id/schedule` routes despite Sprint 4 marking 4.1/4.2 Complete and creating `CoachDetailPage`/`CoachSchedulePage`. Tapping a coach in the list will navigate to a route that 404s back to `/`. Out of scope for 7.1; flag for QA on the 4.x stories.

### File List

**Added (backend)**

- `libs/db/src/entities/membership-plan.entity.ts`
- `libs/db/src/migrations/1778355591678-AddMembershipPlans.ts`
- `apps/api/src/modules/membership-plans/membership-plans.module.ts`
- `apps/api/src/modules/membership-plans/membership-plans.service.ts`
- `apps/api/src/modules/membership-plans/membership-plans.controller.ts` (public)
- `apps/api/src/modules/membership-plans/admin-membership-plans.controller.ts` (admin)
- `apps/api/src/modules/membership-plans/format-duration.ts`
- `apps/api/src/modules/membership-plans/dto/create-plan.dto.ts`
- `apps/api/src/modules/membership-plans/dto/update-plan.dto.ts`
- `apps/api/src/modules/membership-plans/dto/plan-response.dto.ts`
- `apps/api/src/modules/membership-plans/index.ts`
- `apps/api/src/modules/membership-plans/__tests__/membership-plans.service.spec.ts`

**Added (frontend — mini-app)**

- `apps/mini-app/src/pages/PlansPage.tsx`
- `apps/mini-app/src/shared/api/membership-plans.api.ts`
- `apps/mini-app/src/shared/utils/formatRussian.ts`

**Added (frontend — admin)**

- `apps/admin/src/pages/PlansListPage.tsx`
- `apps/admin/src/pages/PlanNewPage.tsx`
- `apps/admin/src/pages/PlanEditPage.tsx`
- `apps/admin/src/features/plans/PlanForm.tsx`
- `apps/admin/src/shared/api/membership-plans.api.ts`

**Modified**

- `libs/db/src/entities/index.ts` (export MembershipPlan)
- `libs/db/src/data-source.ts` (register MembershipPlan in entities array)
- `libs/db/src/entities/__tests__/entities.spec.ts` (3 new MembershipPlan tests)
- `libs/db/src/seeds/seed.ts` (seed 6 default plans, idempotent)
- `apps/api/src/api.module.ts` (register MembershipPlansModule)
- `apps/mini-app/src/app/Router.tsx` (add `/plans` route)
- `apps/mini-app/src/pages/index.ts` (re-export PlansPage)
- `apps/mini-app/src/pages/ClubPage.tsx` (add "Доступные абонементы" link)
- `apps/mini-app/src/shared/api/index.ts` (re-export membershipPlansApi)
- `apps/admin/src/app/Router.tsx` (add `/plans`, `/plans/new`, `/plans/:id` routes)
- `apps/admin/src/app/components/AdminShell.tsx` (add NAV_ITEMS with "Абонементы" link)
- `apps/admin/src/pages/index.ts` (re-export plan pages)
- `apps/admin/src/shared/api/index.ts` (re-export adminPlansApi)

### Change Log Entry

| Date       | Version | Description                                                                                                              | Author |
| ---------- | ------- | ------------------------------------------------------------------------------------------------------------------------ | ------ |
| 2026-05-09 | 1.1     | Implementation complete; smoke test blocked by pre-existing Reminder entity registration bug (Story 5.1 will resolve it) | James  |

---

## QA Results

_(To be filled after dev: gate file at `docs/qa/gates/7.1-membership-plan-catalog.yml`)_
