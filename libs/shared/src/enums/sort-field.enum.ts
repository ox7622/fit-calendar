/**
 * Перечисление доступных полей для сортировки.
 */
export enum ESortField {
    NAME = 'name',
    CREATED_AT = 'created_at',
    UPDATED_AT = 'updated_at',
    IS_ACTIVE = 'is_active',
    SLUG = 'slug',
}
